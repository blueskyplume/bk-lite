# 3.0.0

- `main.go` 新增 `version` 子命令，可直接输出当前执行器版本，且不影响原有 `--config` 启动方式。
- 新增攻击者视角的安全 TDD 覆盖，验证本地执行、SSH 执行、文件下载与解压链路。
- 加固本地执行请求校验，拒绝空命令、非正数超时和不受支持的 shell。
- 加固 SSH 请求校验，在拨号前拒绝非法命令、主机、用户、端口和超时参数。
- 阻止危险下载文件名，避免绝对路径写入与路径穿越。
- 在 ZIP 解压时拒绝符号链接及其他不受支持的特殊文件类型。
