[[inputs.diskio]]
    startup_error_behavior = "retry"
    interval = "{{ interval }}s"
    [inputs.diskio.tags]
        instance_id = "{{ instance_id }}"
        instance_type = "{{ instance_type }}"
        collect_type = "host"
        config_type = "diskio"