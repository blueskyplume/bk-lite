[[inputs.ping]]
    startup_error_behavior = "retry"
    urls = ["{{ url }}"]
    interval = "{{ interval }}s"
    [inputs.ping.tags]
        instance_id = "{{ instance_id }}"
        instance_type = "{{ instance_type }}"
        collect_type = "ping"
        config_type = "ping"
